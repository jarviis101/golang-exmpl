syntax = "proto3";

option go_package = "./";

package auth;

message Payload {
  uint32 type = 1;
  int32 order_id = 2;
}

message PaymentResponse{
  string status = 1;
}

service PaymentService {
  rpc Pay(Payload) returns (PaymentResponse){}
}
